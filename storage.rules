
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Only allow users to read and write their own files in the memories folder.
    match /memories/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
