specVersion: "v1"
serviceId: "kronicl"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "kronicl-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
